# Copy this file to secrets.toml and fill in your values
# DO NOT commit secrets.toml to git!

# Random string for cookie encryption
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
COOKIE_KEY = "your-random-secret-key-here"

# Your app's URL (update for production)
REDIRECT_URI = "http://localhost:8501"
# For Streamlit Cloud: REDIRECT_URI = "https://your-app-name.streamlit.app"

# Admin users - full access, can manage the app
ADMIN_USERS = [
    "jlsimmons33@gmail.com",
]

# Allowed users - can use the app (admins are automatically allowed)
ALLOWED_USERS = [
    "another.user@gmail.com",
    "team.member@company.com",
]

# Google OAuth credentials (paste from your google_credentials.json)
# [google_credentials]
# type = "..."
# project_id = "..."
# etc.
